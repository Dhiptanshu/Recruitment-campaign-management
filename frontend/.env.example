# Base URL of the backend API. Defaults to the local FastAPI dev server if unset.
# In production (Vercel), set this to your deployed Render backend URL, e.g.:
# VITE_API_BASE_URL=https://globalvox-backend.onrender.com
VITE_API_BASE_URL=http://127.0.0.1:8000
